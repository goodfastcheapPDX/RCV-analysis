# Test environment configuration
# These values are used when NODE_ENV=test

# Use test data environment  
DATA_ENV=test

# Use test-specific CSV source
SRC_CSV=tests/golden/micro/cvr_small.csv

# Disable debug output in tests (unless explicitly needed)
DEBUG=false
VERBOSE=false

# Test URLs
NEXT_PUBLIC_BASE_URL=http://localhost:3000
DATA_BASE_URL=http://localhost:8788
TEST_DATA_DIRECTORY="public/data/test"