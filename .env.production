# Production environment configuration
# These values are used when NODE_ENV=production

# Use production data environment
DATA_ENV=prod

# Production CSV sources (full datasets)
SRC_CSV=data/2024-11/canonical/district-2-cast-vote-record.csv

# Disable debug and verbose logging in production
DEBUG=false
VERBOSE=false

# Production URL (Vercel will set VERCEL_URL automatically)
# These will be set as environment variables in Vercel dashboard
# NEXT_PUBLIC_BASE_URL will be set to https://your-domain.vercel.app
# DATA_BASE_URL will be set to https://your-domain.vercel.app
