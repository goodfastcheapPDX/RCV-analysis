# Base environment configuration
# This file contains default values that apply to all environments

# Node.js environment (development, production, test)
NODE_ENV=development

# Data processing environment (dev, test, prod)  
DATA_ENV=dev

# Default source CSV path for data ingestion
# Can be overridden in environment-specific files or via command line
SRC_CSV=data/2024-11/canonical/district-2-cast-vote-record.csv

# Base URL for the application (used in sitemap generation)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Debug and verbose logging flags
DEBUG=false
VERBOSE=false

# Build script defaults (can be overridden via CLI arguments)
# ELECTION_ID=portland-20241105-gen
# CONTEST_ID=d2-3seat
# DISTRICT_ID=d2
# SEAT_COUNT=3