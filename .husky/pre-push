echo "🔄 Pre-push: Syncing data to public/data for deployment..."

if [ -d "data" ]; then
  echo "📁 Syncing data/ → public/data/"
  mkdir -p public/data
  rsync -av --delete data/ public/data/
  echo "✅ Data sync complete"
else
  echo "⚠️  No data/ directory found - skipping data sync"
fi
